---
platform: linux

image_resource:
  type: registry-image
  source:
    aws_access_key_id: ((access_key_id))
    aws_secret_access_key: ((aws_secret_key))
    repository: general-concourse-task
    aws_region: ((region))
    tag: latest

inputs:
- name: cg-deploy-cf
- name: terraform-yaml
outputs:
- name: terraform-secrets

run:
  path: cg-deploy-cf/concourse/terraform-secrets.sh
