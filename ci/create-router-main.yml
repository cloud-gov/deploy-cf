platform: linux

image_resource:
  type: registry-image
  source:
    aws_access_key_id: ((access_key_id))
    aws_secret_access_key: ((secret_access_key))
    repository: general-task
    aws_region: ((region))
    tag: latest

inputs:
- name: cf-deployment
- name: cg-deploy-cf
outputs:
- name: router-main

run:
  path: cg-deploy-cf/ci/create-router-main.sh

