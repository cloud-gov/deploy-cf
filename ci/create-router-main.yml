platform: linux

image_resource:
  type: registry-image
  source:
    aws_access_key_id: ((ecr_aws_key))
    aws_secret_access_key: ((ecr_aws_secret))
    repository: general-task
    aws_region: ((region))
    tag: latest

inputs:
- name: cf-deployment
- name: cf-manifests
outputs:
- name: router-main

run:
  path: cf-manifests/ci/create-router-main.sh

